should = require 'should'
Plugin = require '../lib/plugin'

describe 'Plugin', ->

    it 'is loaded using Nez.plugin()', (done) ->

    describe 'extends the stack functionality', ->

        it "by assigning additional behaviours to the node 'class' names", (done) ->

            