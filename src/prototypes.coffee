module.exports = 

    object:

        set:

            expect: ->

                Object.prototype.expect = ->

            expectSet: ->

                Object.prototype.expectSet = ->

            expectGet: ->

                Object.prototype.expectGet = ->
