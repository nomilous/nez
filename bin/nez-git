#!/usr/bin/env node

path = require('path');
fs   = require('fs');

require(
    path.join(
        path.dirname(
            fs.realpathSync(
                __filename
            )
        ), '../lib/exec/nez-git'
    )
).exec();
