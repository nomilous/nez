// Generated by CoffeeScript 1.4.0
var injector;

injector = require('nezkit').injector;

module.exports = {
  realize: function() {
    var Nez, key, objective, stack, stacker, testFunction, testSubject, validator;
    objective = arguments[0];
    testSubject = injector.support.findModule({
      module: objective
    });
    Nez = require('./nez');
    stack = Nez.link();
    stack.name = objective;
    validator = stack.validator;
    stacker = stack.stacker;
    for (key in arguments) {
      testFunction = arguments[key];
    }
    return injector.inject([testSubject, validator, stacker], testFunction);
  }
};
