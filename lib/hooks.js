// Generated by CoffeeScript 1.4.0
var Hooks, hooks;

require('fing');

Hooks = (function() {

  function Hooks(subscribe) {
    subscribe('edge', this.handle);
    this.hooks = {};
  }

  Hooks.prototype.set = function(hookName, node, callback) {
    var _base, _name;
    (_base = this.hooks)[_name = node.fing.ref] || (_base[_name] = {});
    return this.hooks[node.fing.ref][hookName] = callback;
  };

  Hooks.prototype.handle = function(placeholder, nodes) {
    return console.log(nodes);
  };

  return Hooks;

})();

hooks = {};

module.exports = {
  getFor: function(stack) {
    var _name;
    return hooks[_name = stack.fing.ref] || (hooks[_name] = new Hooks(stack.on));
  }
};
