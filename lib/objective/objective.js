// Generated by CoffeeScript 1.6.3
var Objective, Realize;

Realize = require('../realization/realize');

module.exports = Objective = (function() {
  function Objective() {}

  Objective.prototype.configure = function(opts, done) {
    this.opts = opts;
    return done();
  };

  Objective.prototype.defaultObjective = function(Signature) {
    return Signature('Title', function(done) {
      this.does = 'nothing';
      return done();
    });
  };

  Objective.prototype.onBoundryAssemble = function(opts, callback) {
    return Realize.loadRealizer(opts).then(function(realizer) {
      var phrase;
      phrase = {
        title: realizer.opts.title,
        control: realizer.opts,
        fn: realizer.realizerFn
      };
      delete phrase.control.title;
      opts.loadTree = false;
      return callback(null, phrase);
    }, function(error) {
      return callback(error);
    });
  };

  Objective.prototype.startScheduler = function(monitor, jobTokens, jobEmitter) {
    return console.log('WARNING Objective.startScheduler() undefined override.');
  };

  return Objective;

})();
