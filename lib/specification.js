// Generated by CoffeeScript 1.4.0
var Confirmation, Specification;

require('fing');

Confirmation = require('./confirmation');

Specification = (function() {

  function Specification() {}

  Specification.objects = {};

  Specification.create = function(object, configuration) {
    var confirmation, _base, _name;
    (_base = this.objects)[_name = object.fing.ref] || (_base[_name] = {
      object: object,
      confirmations: []
    });
    confirmation = new Confirmation(object, configuration, function(realization) {
      console.log('REALIZATION:', realization);
      return console.log('TODO: it this realization back into the Confirmation stack');
    });
    this.objects[object.fing.ref].confirmations.push(confirmation);
    return confirmation;
  };

  Specification.getNode = function(stackName) {
    return require('./nez').stacks[stackName].node;
  };

  return Specification;

})();

module.exports = Specification;
